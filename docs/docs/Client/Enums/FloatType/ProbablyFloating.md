# ProbablyFloating

The client is most likely a floating window, unless other factors deny such, treat as floating window.

- [x] **Currently implemented.**

#### Value
```C
enum 
FloatType
{
    ...,
    ProbablyFloating = 1,
    ...,
};
```
