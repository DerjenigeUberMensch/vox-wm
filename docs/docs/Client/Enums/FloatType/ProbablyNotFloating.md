# ProbablyNotFloating

The client is unlikely to be a floating window, unless other factors deny such, treat as regular window.

- [x] **Currently implemented.**

#### Value
```C
enum 
FloatType
{
    ...,
    ProbablyNotFloating = 3,
    ...,
};
```
