# WMMapStateMapped

This is a custom option and is used to set whether to map or unmap a window.

- [ ] **Not implemented.**

#### State
`Mapped` indicates that a window is or should be rendered on to the screen.

#### Value
```C
enum 
WMMapState
{
    ...,
    WMMapStateMapped = 0,
    ...,
};
```