# PropExitThread

Requests that the thread handling this event terminates if not the main thread.

- [x] **Currently implemented.**

#### Value
```C
enum 
PropertyType
{
    ...,
    PropExitThread = 21,
    ...,
};
```
