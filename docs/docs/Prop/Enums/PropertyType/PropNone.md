# PropNone

This enum is not used however represents the first enum in the PropertyType enum.

- [x] **Currently implemented. (unused)**

#### Value
```C
enum 
PropertyType
{
    ...,
    PropNone = 0,
    ...,
};
```
